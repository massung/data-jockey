SELECT ['top', 'new', 'best']
EXPLODE _0

# load the top Hacker News stories for each group
RUN 'hn.pqs', _0

# remove duplicates, sort by score
DISTINCT BY id
SORT BY score DESC

# output them as CSV
WRITE AS CSV
